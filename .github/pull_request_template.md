## Summary

- What does this PR change?
- Why is this change needed?

## Checklist

- [ ] `forge fmt` applied locally (or `forge fmt --check` passes)
- [ ] `forge build` succeeds locally
- [ ] `forge test` succeeds locally
- [ ] No CEI ordering changes that break invariants
- [ ] If events/state shapes changed, tests and docs updated

## Testing notes

- Commands used and key outputs:

```bash
forge fmt
forge build
forge test -vv
```

## Additional context

- Link related issues/PRs, context, or references here.
